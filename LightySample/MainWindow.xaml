<Window x:Class="LightySample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LightySample"
        xmlns:wb="clr-namespace:SourceChord.Lighty;assembly=Lighty"
        wb:LightBox.ItemContainerStyle="{StaticResource closableContainerStyle}"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <wb:LightBox.Template>
        <ControlTemplate>
            <Grid Background="#88000000">
                <ItemsPresenter />
            </Grid>
        </ControlTemplate>
    </wb:LightBox.Template>
    <wb:LightBox.ItemsPanel>
        <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
    </wb:LightBox.ItemsPanel>
    <!--<wb:LightBox.ItemContainerStyle>
        <Style TargetType="{x:Type ContentControl}">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border Width="150"
                                Height="150"
                                Background="Beige"
                                BorderBrush="Black"
                                BorderThickness="2"
                                Margin="5"
                                CornerRadius="3">
                            <ContentPresenter Content="{Binding}" />
                        </Border>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </wb:LightBox.ItemContainerStyle>-->
    <Window.Resources />
    <Grid>
        <Button Margin="50"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Click="button_Click"
                Content="Show LightBox" />
    </Grid>
</Window>
